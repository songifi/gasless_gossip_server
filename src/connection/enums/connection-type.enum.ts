// src/connection/enums/connection-type.enum.ts
export enum ConnectionType {
    FRIENDSHIP = 'friendship', // Bidirectional, requires acceptance
    FOLLOW = 'follow'         // Unidirectional, no acceptance needed
  }