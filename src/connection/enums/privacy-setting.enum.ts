// src/connection/enums/privacy-setting.enum.ts
export enum PrivacySetting {
    PUBLIC = 'public',       // Visible to anyone
    CONNECTIONS = 'connections', // Visible to direct connections only
    PRIVATE = 'private'      // Visible to self only
  }